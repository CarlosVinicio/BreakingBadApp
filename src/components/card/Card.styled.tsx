import styled from '@emotion/styled';

export const Wrapper = styled.section`
  border: solid;
  border-radius: 5px;
  .card {
    cursor: pointer;
    height: 275px;
  }
  .card img {
    width: 100%;
    height: 275px;
  }
`;